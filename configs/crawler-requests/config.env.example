# Прокси по умолчанию (опционально). CLI-флаги --proxy / --proxy-file имеют приоритет.
# Кейс: сайт блокирует зарубежный трафик — впиши российский прокси, и все обходы пойдут через него.
# Формат: http://user:pass@host:port
PROXY=
